---
title: エクスポート
description: Markdown ドキュメントを HTML ファイルとしてエクスポートする方法を説明します。
---

import { Aside } from '@astrojs/starlight/components';
import imgExport from '../../../../assets/images/guides/export-html-01.png';
import imgExportButton from '../../../../assets/images/guides/export-html-button-01.png';

Bokuchi は Markdown ドキュメントをスタイル付き HTML ファイルとしてエクスポートできます。Web ブラウザで開いたり、他の人と共有したりするのに便利です。

## HTML エクスポート

エクスポートされた HTML ファイルには以下が含まれます：

- **スタイル付き** — Bokuchi のプレビューと同じ見た目で出力されます
- **シンタックスハイライト** — コードブロックのカラーハイライトが保持されます
- **画像の参照** — 画像は相対パスのまま出力されます。エクスポートした HTML と同じフォルダ構成で画像ファイルを配置してください
- **変数の展開** — すべての[変数](/ja/guides/variables/)が実際の値に置き換えられます
- **数式と図表** — KaTeX の数式と Mermaid の図表がレンダリングされた状態で出力されます

## エクスポートの手順

1. エクスポートしたいファイルを開きます
2. プレビューパネル右上の**ダウンロードボタン**をクリック、またはメニューからエクスポートを選択します
3. 保存場所とファイル名を選びます
4. `.html` ファイルが保存されます

<img src={imgExportButton.src} alt="プレビューパネルヘッダーのダウンロードボタン" width="700" />

<img src={imgExport.src} alt="エクスポートされた HTML をブラウザで表示した画面" width="700" />

<Aside>
エクスポートされた HTML ファイルは Bokuchi がなくても Web ブラウザで開けます。ただし画像を含むドキュメントを共有する場合は、HTML ファイルと一緒に画像ファイルも同じ相対パス構成で渡してください。
</Aside>

## その他のエクスポート

### 設定のエクスポート / インポート

Bokuchi の設定を JSON ファイルとしてエクスポートしたり、ファイルからインポートしたりできます。設定の複数マシン間での同期やバックアップに便利です。

### 変数のエクスポート / インポート

グローバル変数を YAML ファイルとしてエクスポート、またはインポートできます。チームでの変数共有に便利です。

## 次のステップ

- [変数システム](/ja/guides/variables/) — 変数の使い方を学ぶ
- [設定](/ja/guides/settings/) — Bokuchi をカスタマイズする
