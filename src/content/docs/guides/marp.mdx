---
title: Marp Presentations
description: Create slide presentations from Markdown using the Marp feature in Bokuchi.
---

import { Aside } from '@astrojs/starlight/components';
import imgSplitView from '../../../assets/images/guides/marp-split-view-01.png';
import imgEnableSettings from '../../../assets/images/guides/marp-enable-settings-01.png';
import imgPreviewOnly from '../../../assets/images/guides/marp-preview-only-01.png';
import imgFullscreen from '../../../assets/images/guides/marp-fullscreen-01.png';
import imgThumbnailGrid from '../../../assets/images/guides/marp-thumbnail-grid-01.png';
import imgThemeGaia from '../../../assets/images/guides/marp-theme-gaia-01.png';
import imgThemeFolder from '../../../assets/images/guides/marp-theme-folder-01.png';

Bokuchi can render Markdown files as slide presentations using [Marp](https://marp.app/). Write your slides in Markdown and preview them as a presentation — no separate presentation software needed.

<img src={imgSplitView.src} alt="A Marp presentation in Bokuchi — Markdown in the editor on the left, rendered slides in the center, and the outline panel on the right" width="700" />

## Enabling Marp

Marp is disabled by default. To enable it:

1. Open [Settings](/guides/settings/) with **Cmd+,** (macOS) or **Ctrl+,** (Windows/Linux)
2. Go to **Advanced** > **Rendering Extensions**
3. Turn on **Marp (Presentations)**

<Aside>
When Marp is disabled, its library is not loaded, so there is no impact on editor performance.
</Aside>

<img src={imgEnableSettings.src} alt="The Bokuchi Settings screen with the Marp (Presentations) toggle turned on under Rendering Extensions" width="700" />

## Writing a Presentation

Add `marp: true` to the YAML front-matter at the top of your Markdown file. Separate slides with `---` (three hyphens on their own line).

```markdown
---
marp: true
---

# Slide 1

Welcome to my presentation!

---

# Slide 2

- Point A
- Point B
- Point C

---

# Slide 3

Thank you!
```

When Bokuchi detects `marp: true` in the front-matter, the preview automatically switches to presentation mode.

## View Modes

Marp presentations behave differently depending on the current view mode:

### Split View (Ctrl+Shift+1)

All slides are displayed vertically in the preview pane. The editor and slides scroll together, so you can see which slide you are editing.

### Preview Only (Ctrl+Shift+3)

Slides are displayed one at a time with navigation controls:

| Action | Shortcut |
|--------|----------|
| Next slide | Right arrow / Down arrow / Space |
| Previous slide | Left arrow / Up arrow |
| Exit fullscreen | Escape |

A slide counter at the top shows your current position (e.g., "Slide 2 of 10").

<img src={imgPreviewOnly.src} alt="Preview Only mode showing a single Marp slide with navigation arrows and a slide counter" width="700" />

### Fullscreen

Click the fullscreen button in the preview header to display the current slide in a 16:9 aspect ratio on a dark background. Press **Escape** to exit fullscreen.

<img src={imgFullscreen.src} alt="A Marp slide displayed fullscreen in 16:9 on a dark background with a slide counter" width="700" />

### Thumbnail Grid

Click the grid button in the preview header to see all slides at a glance. Click any thumbnail to jump to that slide.

<img src={imgThumbnailGrid.src} alt="Thumbnail grid showing all slides of a Marp presentation at a glance" width="700" />

## Themes

Marp supports built-in themes. Specify a theme in the front-matter:

```markdown
---
marp: true
theme: gaia
---
```

Available Marp themes include `default`, `gaia`, and `uncover`.

<img src={imgThemeGaia.src} alt="A Marp presentation rendered with the gaia theme, set via theme: gaia in the front-matter" width="700" />

## Custom Themes

Beyond the built-in themes, you can register your own Marp themes from a folder of CSS files.

1. Open [Settings](/guides/settings/) > **Advanced** (Marp must be enabled).
2. Under **Marp Theme Folder**, click **Choose Folder…** and select a folder containing your `.css` theme files.
3. Bokuchi scans the folder and lists the themes it finds. Use **Reload** if you add or change files, or **Clear** to remove the folder.

Each CSS file becomes a selectable theme as long as it starts with an `@theme` header naming the theme:

```css
/* @theme my-theme */

section {
  background: #fdf6e3;
  color: #333;
}
```

Then reference that name in your slide's front-matter, just like a built-in theme:

```markdown
---
marp: true
theme: my-theme
---
```

<img src={imgThemeFolder.src} alt="The Marp Theme Folder card in Settings > Advanced, with the Choose Folder / Reload / Clear buttons and a list of detected theme names" width="700" />

<Aside>
Files without an `@theme` header are ignored — Bokuchi lists them separately so you can see why they weren't picked up.
</Aside>

## Images

Relative image paths in your Markdown work inside Marp presentations. Bokuchi automatically resolves them so that images display correctly in the preview.

```markdown
---
marp: true
---

![Diagram](./images/diagram.png)
```

## Next Steps

- [Editor & Preview](/guides/editor-and-preview/) — Learn about view modes and synchronized scrolling
- [Export](/guides/export/) — Export your documents
- [Settings](/guides/settings/) — Configure rendering extensions
